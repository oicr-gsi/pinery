# This properties file is used to make Maven properties available in Spring xml configuration. 
# Values here are provided via the maven-resources-plugin, and should not be hard-coded.

project.version=${project.version}
project.name=${name}
