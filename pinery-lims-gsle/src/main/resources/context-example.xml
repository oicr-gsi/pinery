<!-- 

This file is used in Tomcat to switch Spring profile and allow properties to be set outside of the packaged webapp. To 
use this file, copy it to $CATALINA_HOME/conf/[engine]/[host]/ and rename it to match the webapp name. E.g. if your 
webapp is pinery-gsle, change the filename to pinery-gsle.xml.

You may need to change the value of gsle.propertiesFile (below) if your properties file will be in a different location.

-->

<Context>
   <Parameter name="spring.profiles.active" value="gsle" override="false"/>
   <Parameter name="gsle.propertiesFile" value="file:${CATALINA_HOME}/conf/Catalina/localhost/gsle.properties" override="false"/>
</Context>
